// import axios, { AxiosRequestConfig } from "axios";

// import { enviroment } from "@/utils";

// import { interceptRequest } from "./interceptors/interceptRequest";
// import { interceptResponse } from "./interceptors/interceptResponse";
// import { onResponseError } from "./interceptors/interceptError";

// const config: AxiosRequestConfig = {
//   baseURL: enviroment.API_URL,
//   timeout: 10000,
// };

// const httpClient = axios.create(config);

// httpClient.interceptors.request.use(interceptRequest);
// httpClient.interceptors.response.use(interceptResponse, onResponseError);

// export default httpClient;
